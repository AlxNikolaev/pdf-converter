{
  "name": "slides-addon-pdf-to-slides",
  "private": true,
  "scripts": {
    "clasp:push": "clasp push",
    "clasp:pull": "clasp pull",
    "clasp:open": "clasp open",
    "clasp:deploy": "clasp deploy --description 'Deploy'",
    "clasp:version": "clasp version 'Update'"
  },
  "devDependencies": {
    "clasp": "^1.0.0"
  }
}
